How to build
------------

- Clone vcpkg and configure environment variable "CMAKE_TOOLCHAIN_FILE"

- Run the following commands in the project root directory:
	- cmake -B build
	- cmake --build build